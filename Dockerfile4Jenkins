FROM maven:eclipse-temurin

RUN groupadd -g 992 jenkins
RUN useradd -r -u 1002 -g jenkins -d /home/jenkins jenkins

RUN mkdir /home/jenkins && mkdir /opt/tessa
RUN chown jenkins:jenkins /home/jenkins && chown jenkins:jenkins /opt/tessa

USER jenkins

WORKDIR /home/jenkins
